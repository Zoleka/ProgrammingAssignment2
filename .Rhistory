getwd
getwd()
setwd("C:/Users/Guest/ProgrammingAssignment2")
m <- NUL
set <- function(y) {
x <<- y
m <<- NULL
}
get <- function() x
setinvert <- function(solve) m <<- mean
getinvert <- function() m
list(set = set, get = get
setinvert = setinvert,
setinvert = setinvert
getinvert = getinvert
makeCacheMatrix <- function(x = numeric(1:4, 2, 2)) {
m <- NULL
set <- function(y) {
x <<- y
m <<- NULL
}
get <- function() x
#Changing it to get the inverse matric not the mean using the solve function
setinvert <- function(solve) m <<- mean
getinvert <- function() m
list(set = set, get = get,
setinvert = setinvert,
getinvert = getinvert)
}
